<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:a="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    a:layout_width="match_parent"
    a:layout_height="match_parent">

    <ImageView
        a:id="@+id/ivUserAvatar"
        a:layout_width="0dp"
        a:layout_height="0dp"
        a:padding="10dp"

        app:layout_constraintBottom_toBottomOf="@id/llUserLabel"
        app:layout_constraintDimensionRatio="w,1:1"
        app:layout_constraintHorizontal_weight="1"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/llUserLabel"
        app:layout_constraintTop_toTopOf="@id/llUserLabel"

        tools:src="@drawable/android_black_rectangle" />

    <LinearLayout
        a:id="@+id/llUserLabel"
        a:layout_width="0dp"
        a:layout_height="wrap_content"
        a:orientation="vertical"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_weight="3"
        app:layout_constraintLeft_toRightOf="@id/ivUserAvatar"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            a:id="@+id/tvUserName"
            a:layout_width="match_parent"
            a:layout_height="wrap_content"

            tools:text="user name" />

        <!--TODO: badge-->

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>